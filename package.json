{
  "name": "document-viewer-monorepo",
  "version": "1.0.0",
  "scripts": {
    "viewer:build": "pnpm --filter @document-kit/viewer run build",
    "viewer:release": "pnpm --filter @document-kit/viewer run release",
    "examples:dev": "pnpm --filter examples run dev",
    "docs:dev": "pnpm --filter docs run dev",
    "docs:build": "pnpm --filter docs run build",
    "docs:serve": "pnpm --filter docs run serve"
  },
  "devDependencies": {
    "vite": "^3.0.2"
  }
}
